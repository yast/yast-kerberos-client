/**
 * File:	include/kerberos-client/dialogs.ycp
 * Package:	Configuration of kerberos-client
 * Summary:	Dialogs definitions
 * Authors:	Jiri Suchomel <jsuchome@suse.cz>
 *
 * $Id$
 */

{

textdomain "kerberos";

import "Kerberos";

include "ui/common_popups.ycp";
include "ui/common_messages.ycp";

include "kerberos-client/helps.ycp";
include "kerberos-client/routines.ycp";

/**
 * Configure1 dialog
 * @return dialog result
 */
global define symbol Configure1Dialog () ``{

    /* Kerberos-client configure1 dialog caption */
    string caption = _("Kerberos-client configuration");

    /* XXPkg configure1 dialog contents */
    term contents = `Label (_("First part of configuration of kerberos-client"));

    Wizard::SetContentsButtons(caption, contents, HELPS["c1"]:"",
	    BackButtonLabel(), NextButtonLabel());

    any ret = nil;
    while(true) {

	ret = UI::UserInput();

	/* abort? */
	if(ret == `abort || ret == `cancel) {
	    if(ReallyAbort()) break;
	    else continue;
	}
        else if(ret == `next || ret == `back) {
            break;
        }
        else {
            y2error("unexpected retcode: %1", ret);
            continue;
        }
    }

    return ret;
}

/**
 * Configure2 dialog
 * @return dialog result
 */
global define symbol Configure2Dialog () ``{

    /* Kerberos-client configure2 dialog caption */
    string caption = _("Kerberos-client configuration");

    /* XXPkg configure2 dialog contents */
    term contents = `Label (_("Second part of configuration of kerberos-client"));

    Wizard::SetContentsButtons(caption, contents, HELPS["c2"]:"",
	    BackButtonLabel(), NextButtonLabel());

    any ret = nil;
    while(true) {

	ret = UI::UserInput();

	/* abort? */
	if(ret == `abort || ret == `cancel) {
	    if(ReallyAbort()) break;
	    else continue;
	}
        else if(ret == `next || ret == `back) {
            break;
        }
        else {
            y2error("unexpected retcode: %1", ret);
            continue;
        }
    }

    return ret;
}

/* EOF */
}
